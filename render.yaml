services:
  - type: web
    name: eia-agent-cliente
    runtime: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /api/health
    envVars:
      - key: EIA_DATA_DIR
        value: /var/data
      - key: EIA_ACCESS_TOKEN
        sync: false
